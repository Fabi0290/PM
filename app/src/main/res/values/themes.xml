<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Trab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/black</item>
    </style>
    <style name="AppTheme.CustomSpinner" parent="Widget.AppCompat.Spinner">
        <item name="android:background">#FFEB3D</item>
        <item name="android:popupBackground">#FFF3E0</item>
        <item name="android:dropDownHorizontalOffset">8dp</item>
        <item name="android:dropDownVerticalOffset">8dp</item>
    </style>

    <style name="Theme.Trab" parent="Base.Theme.Trab" />
</resources>